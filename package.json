{
  "name": "auth-service",
  "version": "1.0.0",
  "description": "Pizzio backend auth service",
  "main": "src/server.ts",
  "scripts": {
    "format:fix": "prettier . --write",
    "format:check": "prettier . --check",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "prettier": "3.3.3",
    "typescript": "^5.6.3"
  }
}
